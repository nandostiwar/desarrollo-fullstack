const fs = require('fs');
const usuariosFile = 'usuarios.json';

function leerUsuarios() {
  // ... (código para leer usuarios del archivo JSON)
}

function guardarUsuarios(usuarios) {
  // ... (código para guardar usuarios en el archivo JSON)
}

function agregarUsuario(usuario) {
  // ... (código para agregar un nuevo usuario)
}

function obtenerUsuarios() {
  // ... (código para obtener todos los usuarios)
}

module.exports = {
  agregarUsuario,
  obtenerUsuarios,
  // ... (otros métodos que necesites)
};
